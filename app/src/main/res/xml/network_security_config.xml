<?xml version="1.0" encoding="utf-8"?><!--
   Release builds talk to the backend over HTTPS only. The debug variant overrides this file to
   allow cleartext, which is where a plain-HTTP server on the local network belongs.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
